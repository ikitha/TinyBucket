<% layout ('layout') %>

<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <% if(!isAuthenticated) { %> <!-- if user is not logged in -->
      <a class="navbar-brand active" href="/new">
      <% } %>
      <% if(isAuthenticated) { %> <!-- if user is signed in -->
        <a class="navbar-brand active" href="/home">
      <% } %>
      <img src="/images/A Tiny Bucket.png">
      TinyBucket</a>
    </div>
     <div class="collapse navbar-collapse navbar-right" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/about">About</a></li>
        <li><a href="/discover">Discover</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="/account/settings">Settings</a></li>
            <li><a href="/account/feed">My feed</a></li>
          </ul>
        </li>
        <li><a href="/logout" class="navbar-nav">Logout</a></li>
      </ul>
  </div>
</nav>

<div class="container main" style="margin-top:50px">
<ol class="breadcrumb">
  <li>Account</li>
  <li><a href="/account/feed">My Feed</a></li>
</ol>

<div class="container">
		<h1>Take A Look Back!</h1>
		<hr />
	</div>
<% for (var i=0; i < currentUserTasks.length; i++) { %> <!-- loop through all the users completed tasks -->
  <div class="blogs">
  	<h2 class="task">Task: <small><%= currentUserTasks[i].dataValues.Task.values.title %></small><span class="glyphicon glyphicon-ok pull-right"></span></h2>
  	<hr />
  	<blockquote class="blog-posts">
    		<p><%= currentUserTasks[i].dataValues.post%></p>
    		<footer><%= currentUserTasks[i].dataValues.updatedAt.toDateString() %></footer>
  	</blockquote>
  </div>
<% } %>

</div>